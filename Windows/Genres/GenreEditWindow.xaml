<Window x:Class="LibraryManager.Windows.Genres.GenreEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Редактирование жанра" Height="300" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Название:" Grid.Row="0" />
        <TextBox x:Name="NameTextBox" Grid.Row="0" Margin="80,0,0,8" MaxLength="50" />

        <Label Content="Описание:" Grid.Row="1" VerticalAlignment="Top" />
        <TextBox x:Name="DescriptionTextBox" Grid.Row="1" Margin="80,0,0,12" MaxLength="500"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="120" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Click="Save_Click" Width="90" Margin="0,0,10,0" />
            <Button Content="Отмена" Click="Cancel_Click" Width="90" />
        </StackPanel>
    </Grid>
</Window>